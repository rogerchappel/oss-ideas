# OSS Factory Run — 2026-05-09 06:30 AEST

Selected five not-yet-built local-first projects for the twice-daily factory cycle.

## Selected ideas

- `gistcaster` — source-linked research capture to Markdown briefs for agents.
- `ledgerpet` — synthetic finance-ops anomaly exercises for safe agent evaluation.
- `limitbar` — local status bar for agent usage limits, spend, and runaway sessions.
- `skilldeck` — portable skill registry, installer, and verifier for agent instructions.
- `tailmux` — Tailscale-aware tmux launcher/status board for local AI nodes.

## Selection notes

- `ideas/ready/ossrank` and `ideas/backlog/worktreeguard` already have local repo directories from recent work, so this run prioritized absent repos that remain unbuilt.
- All selected ideas were already marked `Status: in-progress`; this run note records the active batch without changing their lifecycle path.
- Build agents should create public repos under `rogerchappel/*`, verify locally, and push directly to `main`.
